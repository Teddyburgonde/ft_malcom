=== CONTEXTE DE SESSION - ft_malcolm ===
(à donner à une nouvelle fenêtre Claude pour reprendre où on en est)

ÉTAT D'AVANCEMENT : ~50% de la roadmap (23/46, voir documentation.txt)

CE QUI EST FAIT :
- Sections 1-3 (setup, parsing, validation des arguments) : 100% ✅
- Section 4 (signaux) : 100% ✅
  - Handler Ctrl+C (signal.c) + g_running utilisé dans wait_arp_request, branché
    dans main.c
- Section 5 (socket & réseau) : 2/3 ✅
  - create_raw_socket() (socket.c) : AF_PACKET + ETH_P_ARP + timeout 5s
  - find_network_interface() (network.c) : trouve la carte réseau active (hors loopback)
  - bind_network_interface() (network.c) : bind la socket sur l'interface trouvée
  - ft_strcpy() (network.c, static) : remplace strcpy (pas dans la liste autorisée)
  - Toutes ces fonctions sont appelées dans main.c
  - Reste : tester en conditions réelles sur la VM (pas possible pour l'instant,
    pas d'accès VM)
- Section 6 (attendre une requête ARP) : 100% ✅
  - wait_arp_request() (arp.c) : boucle while(g_running) + recvfrom + gestion
    timeout (EAGAIN/EWOULDBLOCK)
  - Vérifie ntohs(arp->operation) == ARP_REQUEST && arp->target_ip == cfg->source_ip.s_addr
  - Retourne 1 si trouvée, 0 si Ctrl+C
  - Appelée dans main.c : found = wait_arp_request(raw_socket_fd, &cfg);
    if (found) { /* TODO: send_arp_reply */ }
- Section 9 : close(raw_socket_fd) déjà fait dans main.c ✅ (1/4)
- send_arp_reply() : squelette créé dans arp.c (paramètres (void), return (0),
  commentaire "// A FAIRE") pour que ça compile. À implémenter (section 7-8).

CE QUI RESTE À FAIRE :
- Section 7 (la plus grosse partie restante) : implémenter send_arp_reply()
  (arp.c, squelette déjà présent, prototype dans ft_malcom.h)
  - Header Ethernet (dest_mac = MAC de l'émetteur de la requête, src_mac = source_mac
    falsifiée, ethertype = ETH_P_ARP)
  - Paquet ARP : operation = ARP_REPLY (2), sender_mac = cfg->source_mac (falsifiée),
    sender_ip = cfg->source_ip (falsifiée), target_mac/ip = ceux de cfg->target_mac/target_ip
  - Conversion network byte order (htons) pour hw_type, proto_type, operation
  - NOTE : il faudra récupérer sender_mac de la requête ARP reçue (dans
    wait_arp_request ou en relisant un recvfrom) pour le header Ethernet destination
    -> à voir comment passer cette info, ou si on utilise directement cfg->target_mac
- Section 8 : envoyer la trame via sendto + vérifier le retour
- Section 9 : reste "Exiting program...", exit code 0, libération mémoire (peu de
  malloc dans ce projet)
- Section 10 : relecture gestion d'erreurs globale (en grande partie déjà fait au
  fil de l'eau)
- Section 11 : tests réels sur la VM (nécessite l'accès VM)

PRÉFÉRENCES DE TRAVAIL (déjà dans CLAUDE.md) :
- Ne pas écrire les fonctions à la place de l'utilisateur : donner un cours
  (explication + petits exemples), laisser l'utilisateur essayer ~15 min,
  puis du code peut être donné si l'utilisateur a bien compris.
- Tabulations, jamais de "return" direct d'une expression (stocker dans une
  variable avant).
- Avant chaque push, mettre à jour la roadmap dans documentation.txt.

COMMANDES PERSONNALISÉES (.claude/commands/) :
- /explique : "Je t'explique ce que fait le code et tu me dis si c'est bon ou pas"
- /push "message" : commit + push avec ce message
- /context : génère ce fichier de résumé
- /progress : pourcentage d'avancement + estimation du temps restant
