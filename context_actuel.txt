=== CONTEXTE DE SESSION - ft_malcolm ===
(à donner à une nouvelle fenêtre Claude pour reprendre où on en est)

ÉTAT D'AVANCEMENT : ~90% - il ne reste que les tests réels sur la VM Linux.

CE QUI EST FAIT :
- Sections 1-4 (setup, parsing, validation des args, signaux) : 100% ✅
- Section 5 (socket & réseau) : 100% (code) ✅
  - create_raw_socket() (socket.c) : AF_PACKET + ETH_P_ARP + timeout 5s
    (SO_RCVTIMEO), retour de setsockopt VÉRIFIÉ (perror + close + exit)
  - find_network_interface() (network.c) : 1ère interface UP hors loopback,
    buffer static (donc rien à free)
  - bind_network_interface() (network.c) : bind sur l'interface trouvée
- Section 6 (attendre une requête ARP) : 100% ✅
  - wait_arp_request() (arp.c) : boucle while(g_running) + recvfrom
  - Gère EAGAIN/EWOULDBLOCK (timeout) ET EINTR (Ctrl+C) -> continue
  - Filtre : ntohs(arp->operation) == ARP_REQUEST && arp->target_ip == source_ip
- Section 7-8 (construire + envoyer la reply) : 100% ✅
  - send_arp_reply(raw_socket_fd, cfg, interface) (arp.c)
  - Header Ethernet : dest_mac = cfg->target_mac, src_mac = cfg->source_mac
    (falsifié), ethertype = htons(ETH_P_ARP)
  - fill_arp_packet() (static, arp.c) : operation = ARP_REPLY, sender = IP/MAC
    falsifiés, target = cfg->target_*, htons sur les champs 2 octets
  - eth + arp collés dans buffer via ft_memcpy, envoyés via sendto sur une
    struct sockaddr_ll (sll_ifindex = if_nametoindex(interface))
- Section 9 (quitter proprement) : 100% ✅
  - main.c : if (found) send_arp_reply(...); puis printf("Exiting program...\n");
    puis close(raw_socket_fd); return (0);
  - Pas de malloc dans le projet -> rien à libérer
- Section 10 (relecture gestion d'erreurs) : 100% ✅ (faite cette session)
  - #1 Ctrl+C instantané : signal.c passé de signal() à sigaction SANS SA_RESTART
    (sa_flags = 0) -> recvfrom s'interrompt tout de suite ; EINTR géré dans arp.c
  - #2 Message IP : retiré le "." en trop -> conforme exact au sujet
    "ft_malcolm: unknown host or invalid IP address: (%s)"
  - #3 setsockopt : retour désormais vérifié dans socket.c
  - Vérifié : pas de segfault/overflow, pas de fuite, tous les retours testés

UTILS (créé cette session) :
- utils.c : ft_strcpy (déplacé depuis network.c, plus static) + ft_memcpy
  (copie binaire, ne s'arrête pas aux \0). Prototypes dans ft_malcom.h, dans le Makefile.

MESSAGES D'AFFICHAGE (alignés sur l'exemple du sujet, page 9) :
- main.c : "Found available interface: %s"
- wait_arp_request : "An ARP request has been broadcast." + "mac address of
  request: ..." (sender_mac formaté) + "IP address of request: ..." (inet_ntop)
- send_arp_reply : "Now sending an ARP reply..." avant sendto + "Sent an ARP
  reply packet, you may now check the arp table on the target." après
- main.c : "Exiting program..." à la fin

MESSAGES D'ERREUR EXACTS (vérifiés sur le PDF du sujet) :
- IP : "ft_malcolm: unknown host or invalid IP address: (X)." -> AVEC point final
- MAC : "ft_malcolm: invalid mac address: (X)" -> SANS point final
- (le CLAUDE.md notait l'IP sans point : c'est une erreur, suivre le PDF)

NORME : le sujet ne mentionne PAS la Norm de 42. Organisation libre, commentaires
// autorisés. Correction humaine.

STRUCTURES :
- t_arp_packet (28 o) et t_ethernet_frame (14 o) ont __attribute__((packed))
  -> pas de padding, buffer = 42 octets exact sur le réseau.

CE QUI A ÉTÉ TESTÉ CETTE SESSION (sur la VM Debian de l'école) :
- Étape 1 (permissions) : ✅ non-root rejeté, root sans args -> usage affiché
- Étape 2 (spoof + table ARP cible) :
  - Échec sur le réseau physique de l'école (host <-> VM, via ifconfig/ip addr) :
    la vraie réponse ARP du noyau de la VM écrasait la fausse reply -> la table
    ARP du host gardait le vrai MAC. Probable protection réseau de l'école
    (switch avec Dynamic ARP Inspection / port security) qui bloque ou
    neutralise les trames forgées.
  - ✅ Réussi en environnement Docker isolé (voir plus bas) : table ARP de
    `target` empoisonnée avec le faux MAC, confirmé par `ip neigh show`.

ENVIRONNEMENT DE TEST DOCKER (créé cette session, inspiré du repo externe
"kiroussa" qui utilise la même approche) :
- Dockerfile (racine du projet) : image Alpine, installe build-base +
  linux-headers + iputils + iproute2 + tcpdump, copie le projet, compile avec
  `make fclean && make` (fclean obligatoire : sinon les objets/binaire déjà
  compilés sur la VM (glibc) sont copiés tels quels et `make` ne les
  recompile pas pour Alpine/musl -> "not found" à l'exécution)
- docker-compose.yml : 2 conteneurs sur un réseau bridge isolé dédié
  (172.20.0.0/24, sans lien avec le réseau de l'école) :
  - `attacker` (172.20.0.10) : lance ft_malcolm
  - `target` (172.20.0.20) : la victime, dont on vérifie la table ARP
  - cap_add NET_ADMIN + NET_RAW sur les deux (raw sockets + gestion ARP)
- Cibles Makefile ajoutées (en plus des règles obligatoires all/clean/
  fclean/re, inchangées) : `drun` (build --no-cache + up -d), `dattacker`
  (shell dans attacker), `dtarget` (shell dans target), `ddown` (down)
- Test réalisé : IP usurpée (source_ip) = 172.20.0.50, une IP inutilisée par
  aucun conteneur (évite la concurrence avec une vraie réponse, contrairement
  au test école où source_ip = IP réelle de la VM)
  -> `./ft_malcolm 172.20.0.50 aa:bb:cc:dd:ee:ff 172.20.0.20 <mac target>`
  -> ping depuis target vers 172.20.0.50 déclenche bien la requête, ft_malcolm
     répond, `ip neigh show 172.20.0.50` sur target affiche le faux MAC ✅
- kiroussa/ (dossier présent dans le repo local mais volontairement PAS
  suivi par git, sur demande de l'utilisateur - c'est le repo perso d'une
  autre personne ayant fini le projet, gardé seulement comme référence)

CE QUI RESTE À FAIRE :
  - Tester le comportement avec tcpdump (la reply ne doit partir qu'après une
    requête ARP sur le réseau, pas nécessairement de la cible) - tcpdump déjà
    installé dans l'image Docker, test pas encore fait
  - Tester arguments invalides (messages d'erreur exacts) - pas encore fait
  - Tester Ctrl+C pendant l'attente (doit quitter immédiatement) - pas encore
    fait (note : possible dans le conteneur attacker avec Ctrl+C classique)

PRÉFÉRENCES DE TRAVAIL (déjà dans CLAUDE.md) :
- Cours d'abord (explication + petits exemples), laisser l'utilisateur essayer
  ~15 min, puis du code peut être donné si bien compris. Aller à l'essentiel.
- Tabulations, jamais de "return" direct d'une expression.
- Avant chaque push : mettre à jour documentation.txt (roadmap) et context_actuel.txt.

COMMANDES PERSONNALISÉES (.claude/commands/) :
- /explique, /push "message", /context, /progress
